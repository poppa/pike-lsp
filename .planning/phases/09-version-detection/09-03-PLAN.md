---
phase: 09-version-detection
plan: 03
type: execute
wave: 3
depends_on: ["09-02"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Health check command displays actual Pike version"
    - "System handles missing or failed version detection gracefully"
---

<objective>
Verify the end-to-end functionality of Pike version detection and ensure it appears in the user-facing health check.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-version-detection/09-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add E2E test for version detection</name>
  <files>packages/vscode-pike/src/test/feature/version.test.ts</files>
  <action>
    Create a simple E2E test that verifies the health status.
    Since health status is displayed via a command that returns a string, we can test the command result.

    Test pattern:
    ```typescript
    const result = await vscode.commands.executeCommand('pike.lsp.showDiagnostics');
    expect(result).to.contain('Pike Version: 8.0');
    ```
  </action>
  <verify>Run the test: `cd packages/vscode-pike && pnpm run test:features` (ensure the grep matches the new test)</verify>
  <done>Version detection is verified E2E</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Manual Health Check Verification</name>
  <what-built>Pike Version Detection in Health Check</what-built>
  <how-to-verify>
    1. Open VSCode with the Pike extension.
    2. Run command "Pike LSP: Show Diagnostics".
    3. Verify that it shows "Pike Version: 8.0.x" (or similar) instead of "Unknown".
    4. Check the "Pike Language Server" output channel for the detected version log.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Ensure all E2E tests pass.
</verification>

<success_criteria>
Pike version is correctly displayed in the UI/Health check and logged during startup.
</success_criteria>

<output>
After completion, create `.planning/phases/09-version-detection/09-03-SUMMARY.md`
</output>
